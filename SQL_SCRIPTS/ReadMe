So as to not spread insanity.

Each set of reports has it's own folder.
Each report folder has an SQL folder which has the queries used to get the data.
Each report folder has an ARCHIVE of data by month.


